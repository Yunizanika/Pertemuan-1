fun main() {
    var varBool : Boolean = true
    if(varBool)
        println("varBool bernilai " + varBool)


    // boolean yang memiliki nilai true dan false,
    // nilai 1 dan 0 tidak berlaku untuk menunjuk nilai pada sebuah variabel bertipe boolean.
}